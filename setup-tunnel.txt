brew install cloudflared
cloudflared tunnel login
cloudflared tunnel create homeassistant-tunnel
cloudflared tunnel create reactapp-tunnel
After creation, credentials are saved in ~/.cloudflared/ and find .json file
Configure Tunnel Routing at ~/.cloudflared/config.yaml or <filename>.yml
Set Up DNS in Cloudflare as home.yourdomain.com → CNAME to your-tunnel-id.cfargotunnel.com


create tunnel at cloudflare zero trust >> Networks >> Connectors
copy token and update docker
tunnel configure >> published application routes >> create route >> create sub domain 
