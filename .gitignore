.env
.env.local

# CRITICAL: Files/Folders created by HA that must NOT be tracked by Git

# Database and Log files (change constantly)
homeassistant/config/*.db
homeassistant/config/*.log

# User Data, Integrations, and Credentials
homeassistant/config/.storage/
homeassistant/config/secrets.yaml

# Configuration Backups/Caches that don't need to be versioned
homeassistant/config/.HA_VERSION
homeassistant/config/deps/
homeassistant/config/home-assistant_v2.db-shm
homeassistant/config/home-assistant_v2.db-wal
homeassistant/config/customize.yaml  # Often generated by UI, can ignore if you don't use it

setup-*.txt